Step: W1
Done: Five parts in parts/<name>/part.yaml with agents beside skills. registry.ts loadParts() via Bun.YAML. Rename to factory: bin, banner, FACTORY_ROOT, .factory-manifest.json with ivy fallback, ~/.factory/projects seeded from .projects. Home init refuses on Droid leftovers. New update command. Deleted cycle/, mcps/, dropped parts, sounds, @modelcontextprotocol/sdk. CLAUDE.md and README rewritten. Commits def7acc e0c533b ed565ef.
Undone: none
Commands: bun x tsc --noEmit -> 0; status /opt/ed/ivy -> 0 five parts; update /opt/ed/ivy -> 0 removed brainstorm, cycle, sounds; update on temp repo with .ivy-manifest.json -> 0 renamed; bun install -> 0
Issues: .claude/skills/{critic-self,dry,fork-critic} are dangling legacy symlinks now owned by nobody, human to delete. Bun.file().exists() is false for directories, use lstat.
Deviations: update only removes live symlinks into FACTORY_ROOT, else reports left in place. Dropped parts never remove a target a surviving part owns. loadParts() skips folders without part.yaml. Update added to the interactive menu. docs/ivy-screenshot.png renamed factory-screenshot.png. zod left in package.json, unused.
Faster: record each file's source in the manifest so update can verify ownership without readlink.
Acceptance: A-CLI-1 pass, A-CLI-2 pass, A-CLI-3 pass, A-CLI-4 pass, A-CLI-5 pass
