Step: W2
Done: workflows/*.yaml (5). core/workflow.ts load, override by name, recipes, validation, transitions. core/mission.ts folder, state.json, claim, branch, worktree, resolve via git worktree list, close, projects registration, liveness via events mtime. commands mission|step|gate|handoff, core/args.ts, cwd-based, no picker, no banner. status gains Missions block. Rendering per design.md. Refusal error, one line, exit 1. CLAUDE.md updated. Commits ffa626f b828367 37461ea 07d35ae.
Undone: mission open (W3).
Commands: bash /tmp/factory-w2-check.sh -> 0, 110 assertions pass. Full story walk, two-mission worktree case, resume from worktree, liveness, bad loop.back -> 1. bun x tsc --noEmit -> 0.
Issues: git branch --merged reports an empty mission branch as merged, so close postcondition is merged AND folder committed on trunk. Close restores the prior checkout unless it was the closed mission's branch. .factory/claim added to .gitignore. Bun cold starts under /var/folders are ~8s, use /tmp for timing.
Deviations: status/resume/close take an optional name resolved via FACTORY_MISSION, worktree, claim, single open mission. --mission on step/gate/handoff, --all on list. adopt --force --reason to rebind a live session. Local date for folder. --worktree on unclaimed checkout writes no claim. Workflow falls back to default when workflow.yaml missing. No banner for mission family.
Faster: one Bun script importing command functions instead of ~110 CLI cold starts; could become a real test.
Acceptance: A-MIS-1..9 pass, A-REC-2 pass
