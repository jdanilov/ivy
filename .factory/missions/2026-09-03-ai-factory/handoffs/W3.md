Step: W3
Done: presets/{orchestrator,quick,research}/ with preset.yaml, placeholder prompt.md, settings.json overlay, mcp.json. core/spawn.ts: loadPreset, assembleCommand, Warp tab config TOML, openSession; session id written to state.json and claim before the tab opens; --dry-run. mission open [name] [--preset] [--dry-run]; mission new calls it unless --no-open. parts/hook-factory fixture on seven events: events jsonl, binding via FACTORY_MISSION or claim, PreCompact focus, caffeinate keyed by session, SubagentStop saves handoff from last_assistant_message. HookConfig.event widened, matcher optional. Commits e2713c4 d90c135 a8de7c7 c4eb7ba.
Undone: preset prompts are placeholders (W5). plugins/factory absent (W4), missing --plugin-dir is ignored silently by claude.
Commands: tsc -> 0; mission open --dry-run -> 0 all flags; WARP_MISSING=1 open -> 0 prints command; real open in /tmp repo -> Warp tab with claude running; seven hook payloads -> seven lines; caffeinate start/stop verified, unbound never starts; status live/no session verified; chore smoke mission new/steps/close -> 0.
Issues: user-level permissions.deny outranks --settings allow, so ~/.claude/settings.json deny of SendMessage blocks the orchestrator; human must remove it. Global caffeinate hook now redundant. SubagentStop last writer wins per step, an Investigator mid-implement overwrites the Worker handoff. Claim fallback does not walk out of a worktree, FACTORY_MISSION covers spawned sessions.
Deviations: Warp Tab Configs (TOML, [[panes]]) are the current format, Launch Configurations are legacy; directory created. No env key in tab configs, FACTORY_MISSION inlined in the command. SubagentStop prefers last_assistant_message, falls back to agent_transcript_path. Command printed on every open. open writes session into the claim. Orchestrator settings set disableRemoteControl false.
Faster: hooks shorthand in part.yaml { events: [...], command } to replace seven near-identical entries.
Facts: hook stdin keys per event confirmed on 2.1.257 (SessionStart sends source not reason; SubagentStop carries agent_transcript_path and last_assistant_message). Sources: code.claude.com/docs/en/hooks, docs.warp.dev/terminal/windows/tab-configs, docs.warp.dev/terminal/more-features/uri-scheme.
Acceptance: A-SPN-1..4 pass, A-EVT-1..3 pass
