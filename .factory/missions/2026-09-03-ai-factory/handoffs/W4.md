Step: W4
Done: plugins/factory: plugin.json, hooks/hooks.json modules, hooks.ts registering ask and gate as in-process MCP tools on engine.create, answered by tool.call hooks. ask races $.ui.ask against a once-per-second poll of <project>/.factory/inbox/<id>.json, overwrites with { answered: true }. gate draws accept/amend/reject, writes <mission>/gates/<step>.json and returns the factory gate answer command. Status row on engine.create, prompt.submit, turn.complete. types.d.ts loose. test.md four steps. API.md corrected with the engine event set. Commits 7b2a2cc a4bf159 2b55fc2.
Undone: PreCompact function hook, the module API has no compaction event. A-HOOK-1..3 await the human test. mission skill tools section left to W5.
Commands: tsc -> 0; claude plugin validate --strict plugins/factory -> passed (hooks: engine.create, prompt.submit, turn.complete, tool.call; calls: fs.listDir, fs.readFile, fs.writeFile, session.cwd, tool.register, ui.ask, ui.log, ui.status); claude --plugin-dir plugins/factory plugin details factory -> 0, Hooks (0) for module-only plugins; claude -p load check -> tools listed, no mcp__factory__ask, no plugin error. -p is not a REPL host, no registrar.
Issues: validate only checks verbs for known namespaces, unknown events validate green and never fire. $.ui.ask option shape, $.fs return shapes, tool.call payload field unconfirmed, readers tolerant. Poll capped at 15 minutes then "no answer".
Deviations: module events are the engine set: engine.create, prompt.submit, turn.complete, tool.call, PreToolUse. $ cannot be passed to functions, all ops sit in hook bodies. Inbox at <project>/.factory/inbox since $.fs is project-confined.
Faster: map the API with one-line throwaway modules through claude plugin validate, a second each.
Recommendation: keep parts/hook-factory command hooks for PreCompact and the event log, the module API cannot do either.
Acceptance: A-HOOK-1 unchecked, A-HOOK-2 unchecked, A-HOOK-3 unchecked
