Step: W6
Done: update /opt/ed/ivy up to date. igs dogfooded: generic parts relinked to parts/<name>/, manifest renamed, docs-format, hook-factory, mission, terminology, validate, verify installed, afplay hook and sounds removed, browse and permissions skipped via new update --skip with sticky manifest.skipped and a skipped status. skipIfExists templates copied not symlinked. README command table, CLAUDE.md matches tree. Smoke chore mission run and closed, merge 55c5a7f on main. Commits 45b3ca9 bd3a881 effb324.
Undone: none
Commands: update ivy -> 0 twice; update igs --skip browse,permissions -> 0 (6 installed, 5 relinked, 4 removed), rerun up to date; status igs -> 11 installed, 0 modified, 2 skipped; smoke new/steps/close -> 0; git log main --merges -1 -> 55c5a7f; tsc -> 0
Issues: removeHooks reports "hook removed" even when nothing matched (igs had a hand-edited afplay command). close leaves the mission branch behind. Closing from a worktree checks main out in the primary checkout mid-run, a crash strands it.
Deviations: smoke mission pre-branched from main with --worktree, since no claim existed and branching from mission/ai-factory would have merged the whole mission. Smoke change was a real one (git rm --cached .idea) because an empty branch reads as merged. --skip persisted in the manifest, new skipped PartStatus, install removes a picked part from the list. Terminology template copied and reseeded in igs.
Faster: update --skip and part.yaml requires are one idea, a part could declare the project may own it.
igs: no drift, all generic parts were dangling symlinks. igs working tree has uncommitted .claude changes for the human to commit.
Acceptance: A-DOG-1..3 pass
