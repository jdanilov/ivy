
## r0 Validator

Driver: the CLI itself. There is no `.factory/factory.yaml` in this repo, so no `e2e.ready` recipe to
run — the app is a CLI, so the CLI in temp repos under `/tmp` is the driver. Every run used
`HOME=/tmp/valhome.MMhr` (scratch), seeded with the same `~/.factory/config.yaml` the machine carries:

```
vars:
  codegraph: codegraph
```

`install` and `uninstall` were driven for real under a pty (`script -q /dev/null`, W1's incantation),
with the multiselect steered by seeding `manifest.skipped` so only the part under test is preselected,
and by arrow-key + space for the `default: false` parts (codegraph, archify).

A second checkout at `/tmp/ivyco` (`git clone -b mission/refit /opt/ed/ivy`, `node_modules` symlinked)
carried the three throwaway recipe parts A-RCP-1 and A-RCP-3 need. `/opt/ed/ivy` was never written to.

`/opt/ed/igs` was read only — no `update`, no `status` side effects, nothing changed there.

### Contract — validate-kind assertions

| id | result | evidence |
|----|--------|----------|
| A-SNP-1 | pass | `/tmp/snp1b`, `AGENTS.md` with `## Notes` and no `## Important Files`, roadmap alone installed. `git diff --numstat` -> `3	0	AGENTS.md`; the diff is one blank plus `## Important Files` plus `- Roadmap: @docs/roadmap.md`, appended at EOF. Manifest records `snippet: {file: AGENTS.md, section: "## Important Files", line: "- Roadmap: @docs/roadmap.md"}`. Full pty `install` on `/tmp/snp1.89243` with all 13 defaults gave 5 insertions: one blank, the heading, and one line per snippet part |
| A-SNP-2 | pass | `/tmp/snp2a` had only `CLAUDE.md` (with an existing `## Important Files`): the line landed at the end of that section in `CLAUDE.md`, `ls` shows no `AGENTS.md`. `/tmp/snp2b` had neither file: `AGENTS.md` was created holding exactly `## Important Files` + the line, `grep -c` -> 1 |
| A-SNP-3 | pass | `/tmp/snp3`, `AGENTS.md` md5 `8579dcb4…` before. pty `install` -> 13 parts, 3 snippet lines, 1 section. Second pty `install` -> `10 updated`, md5 unchanged `bdc93ecb…`, `grep -c` still 1/1/1 and 1 section. pty `uninstall` (13 parts) -> `cmp /tmp/snp3.orig AGENTS.md` silent, md5 back to `8579dcb4…` |
| A-RCP-1 | pass | `/tmp/rcp1`, scratch part `rcpfail` with init `pwd > init-cwd.txt`, `echo line2-ran > init-line2.txt`, `exit 3`. Output echoed all three `$ …` lines then `✗ rcpfail init: exit 3 -> 3`. `init-cwd.txt` holds `/tmp/rcp1` (each line runs in the project root). `.claude/rcpfail.txt` is still a symlink into `parts/rcpfail/dummy.txt`, manifest entry present with no `initAt`. Rerun as `update` -> exit 1 with the same `✗` (pty masks install's own code; `update` is the same code path) |
| A-RCP-3 | pass | `/tmp/rcp3`, two scratch parts. pty `uninstall`: `rcpuninit` echoed `$ pwd > uninit-cwd.txt` / `$ echo uninit-ran > uninit-ran.txt` then `✓ rcpuninit removed .claude/rcpuninit.txt` (`uninit-cwd.txt` = `/tmp/rcp3`). `rcpuninitfail` printed `◈ rcpuninitfail uninit: exit 7 -> 7` and still `✓ rcpuninitfail removed .claude/rcpuninitfail.txt`. `.claude/` left empty, manifest deleted |
| A-VAR-2 | pass | `/tmp/cg1` with codegraph installed at `vars.codegraph: codegraph`. Edited scratch `~/.factory/config.yaml` to `codegraph: /usr/local/bin/mycg --alpha`, ran `update` -> 0, `codegraph relinked`. `.mcp.json` became `{"type":"stdio","command":"/usr/local/bin/mycg","args":["--alpha","serve","--mcp"]}` (prefix split honoured) and the hook became `CODEGRAPH="/usr/local/bin/mycg --alpha" $CLAUDE_PROJECT_DIR/…`. Exactly one `UserPromptSubmit` block — the stale hook was removed, not duplicated. Reverting the var and re-running `update` restored both |
| A-STB-1 | pass | `/tmp/stb1`, `mission new demo --stub --workflow chore` -> 0, printed `branch  stub — open it to branch`. `find .factory` shows `missions/2026-09-05-demo/{workflow.yaml,intent.md,state.json,handoffs/}`. `state.json`: `"status": "stub"`, `"branch": null`, `"session": null`, `"step": "grill"`. `git branch --list` -> `* main` only. No `.factory/claim` |
| A-STB-2 | pass | Same stub: `step start grill`, `gate open grill --file …`, `mission close demo` and `handoff save grill` each printed exactly one line, `✗ mission demo is a stub, open it first`, and exited 1 |
| A-STB-3 | pass | `mission open demo --dry-run` -> 0, printed the session id, cwd, would-write tab config path and the full `claude …` command. After it: `git branch --show-current` -> `mission/demo`, `git branch --list` has `main` and `mission/demo`, `state.json` `"status": "open"`, `"branch": "mission/demo"`, and `.factory/claim` holds `{"mission":"demo","session":null,…}` |
| A-CLS-1 | pass, with a precondition the Factory does not ship | `/tmp/stb1`, chore mission walked `grill`/`implement`/`merge`, mission folder untracked. First `mission close demo` -> exit 1, `✗ dirty outside the mission folder, commit or stash first: .factory/claim`. After adding `.factory/claim` to the project `.gitignore`, `mission close demo` -> 0 and `git log --graph`: `8531f2c 📦 chore: close mission demo` on `mission/demo`, then `ba537c0 🔀 merge: mission/demo`, then `f8ad6dd 🏗️ chore: close mission demo` on `main`. Close commit before the merge, as claimed. Tree clean, back on `main`, `state.json` `"status": "closed"`. See F1 |
| A-CLS-2 | pass | `/tmp/cls2`, chore mission open on `mission/c2`, then `README.md` modified and `extra.txt` created. `mission close c2` -> exit 1, `✗ dirty outside the mission folder, commit or stash first: README.md, extra.txt` — both paths named, still on `mission/c2`, nothing committed |
| A-PRT-5 | pass | `HOME=<scratch> bun src/cli.ts status /opt/ed/ivy` -> 0. Rows present: `roadmap fixture ● installed docs/roadmap.md`, `/retro skill ● installed`, `codegraph mcp ○ not installed`, `/archify skill ○ not installed`. No `critic` row. Footer `14 installed, 0 modified, 2 available` — no `modified`, no `conflict` |
| A-CG-1 | pass | `/tmp/cg1` with one `index.ts`, codegraph selected alone under a pty. `codegraph init` ran (`Indexed 1 files`, `2 nodes, 1 edges`). `.codegraph/` holds `codegraph.db` (163840 B) and `.gitignore`. `.mcp.json` -> `{"type":"stdio","command":"codegraph","args":["serve","--mcp"]}` (resolved, not `npx`). `.claude/settings.local.json` hook -> `CODEGRAPH="codegraph" $CLAUDE_PROJECT_DIR/.claude/scripts/codegraph-gate.sh`. `.claude/settings.json` -> `permissions.allow: ["mcp__codegraph__*"]`. `.claude/scripts/codegraph-gate.sh` is a symlink into `parts/codegraph/gate.sh`. Manifest records `initAt` and the resolved `uninit` |
| A-CG-2 | pass | Same repo, pty `uninstall`: echoed `$ codegraph uninit --force`, tool printed `✓ Removed CodeGraph from /private/tmp/cg1`, then `✓ codegraph removed .claude/scripts/codegraph-gate.sh`. After: `.codegraph` gone, `.mcp.json` -> `{"mcpServers": {}}`, `settings.local.json` -> `{}`, `settings.json` -> `{}`, `.claude/scripts/` gone, manifest deleted. See F5 |
| A-ARC-2 | pass | `/tmp/arc2`, archify selected alone under a pty. Init echoed the guard, the clone+move line and `node .claude/skills/archify/bin/archify.mjs doctor`; the doctor printed `Archify is ready.` and install reported `Done. 1 part installed.` Run again by hand: `node .claude/skills/archify/bin/archify.mjs doctor` -> exit 0, 15 `[ok]` lines. Tree is 5.8 MB / 80 files, no `.git` inside. `status` -> `/archify ● installed`. pty `uninstall` echoed `$ rm -rf .claude/skills/archify .claude/skills/.archify-clone` -> `.claude/skills/archify` gone, `.claude/skills` empty. See F6 |
| A-DOG-1 | pass, on the amended wording | Read-only inspection of `/opt/ed/igs` plus `handoffs/W5.md`. `.claude/skills/critic` does not exist and `critic` is not in the manifest. `.claude/skills/retro/skill.md` -> symlink to `../../../../ivy/parts/retro/skill.md`; `roadmap` and `retro` are both manifest entries. igs `agents.md` `## Important Files` ends with the three added lines `- Docs format: @.claude/docs-format.md`, `- Terminology: @docs/terminology.md`, `- Roadmap: @docs/roadmap.md`, and the manifest records the `{file, section, line}` for each. igs' own `docs/roadmap.md` is untouched: 25239 B, 419 lines, mtime Sep 4 13:10 (`skipIfExists` held). `status /opt/ed/igs` -> `12 installed, 0 modified, 1 available, 2 skipped` with exactly one `✗ conflict` row, `codegraph .claude/scripts/codegraph-gate.sh`, the expected hand-wired case. The template `docs/roadmap.md` was not created because igs owns that path — the wording the Orchestrator amended. See F2 |
| A-HND-2 | pass | `handoffs/W6.md` carries `Step`, `Done` ×8, `Commands` ×2, `Issues` ×4, `Deviations` ×3, `Faster`, `Acceptance` and omits `Undone` entirely. `handoffs/W5.md` carries `Step` and `Done` ×6 and omits every other field, putting its deviations and issues in prose sections instead. Neither file writes `none` as a placeholder — the only `none` in either is inside the git flag `--filter=blob:none` in W6's `Faster` line. See F3 |
| A-RET-4 | unchecked | Owned by the human in the orchestrator session, per the assertion and my brief. Not run |

### Findings

| id | verdict | finding | blast | effort | conf |
|----|---------|---------|-------|--------|------|
| A-CLS-1 | F1 | `mission close` refuses on its own `.factory/claim`. The Factory writes that file, then counts it as "dirty outside the mission folder" and blocks the close. No part ships a `.gitignore` line for it, so on a fresh project `mission close` can never succeed until the human works out that the blocker is a Factory-generated file. `/opt/ed/ivy` only escapes because its `.gitignore` was hand-edited. Fix: have the dirty check ignore `.factory/claim`, or ship the ignore line from a part | wide | S | high |
| A-DOG-1 | F2 | Snippet dedupe is exact-string, so a project with a hand-written `## Important Files` gets duplicates. igs `agents.md` now reads `- Terminology: \`docs/terminology.md\`` and `- Roadmap: \`docs/roadmap.md\`` immediately followed by `- Terminology: @docs/terminology.md` and `- Roadmap: @docs/roadmap.md` — the same two files listed twice in the same section, in two notations. Every project that already documents its own important files will hit this on first install | wide | M | high |
| A-HND-2 | F3 | `handoffs/W5.md` has no `Acceptance:` line at all. The template makes `Step`, `Done`, `Acceptance` mandatory, and A-DOG-1/A-DOG-2 were W5's to call — their verdicts exist only as prose. The optional-field rule was followed; a required field was dropped | narrow | S | high |
| - | F4 | `--skip` is wired only to `update`. `src/cli.ts:38-58` passes it in the `update` branch and nowhere else, so `install --skip a,b`, `uninstall --skip …` and `status --skip …` accept the flag and ignore it silently. `AGENTS.md:97` documents it for all four commands (`install \| uninstall \| status \| update [project] [--skip a,b]`); `README.md:36` lists it only on `update` and is right. Observed directly: `install /tmp/snp1.89243 --skip <15 parts>` installed all 13 defaults and showed every part as `○ not installed`, never `⊘ skipped`. Predates this mission, carried into `AGENTS.md` by the rename. Fix the doc line or wire the flag | narrow | S | high |
| - | F5 | `uninstall` leaves empty shells: `.mcp.json` as `{"mcpServers": {}}`, `.claude/settings.json` and `.claude/settings.local.json` as `{}`, and an empty `docs/` after a snippet-part removal. `A-SNP-3`'s byte-identical guarantee covers the agent file only. Cosmetic, but a project that had no `.mcp.json` before install now has one forever | narrow | S | high |
| - | F6 | A-ARC-1's `files: []` plus recipe design means archify's 5.8 MB / 80 files land inside `.claude/skills/archify` with nothing ignoring them. A project that tracks `.claude/` — igs does — takes the whole vendored skill into its index on install. W6 already flagged this; confirming it holds in practice. A `.gitignore` line in the init recipe would cost one line | narrow | S | high |
| - | F7 | `mission open <stub> --dry-run` is not dry. It creates the branch, checks it out, writes `.factory/claim` and flips `status` to `open` before printing the command it "would" run. A-STB-3 pins exactly this, so it is the contract, not a defect — but `--dry-run` is the wrong name for a flag that branches your checkout, and the next reader of that flag will be surprised. Rename or split the promote out | narrow | S | med |
| - | F8 | Transient, already cleaned up mid-run: `parts/zz-scratch/` (W1's throwaway part, untracked) was present in the registry at the start of this round and showed in the install menu of every project. It disappeared before I finished. Worth a habit of removing scratch parts from `parts/` in the same step that creates them, since untracked means the Verifier's diff never sees them but the running CLI does | narrow | S | high |

### Tooling

There is no `.factory/factory.yaml` in this repo, so neither `e2e.ready` nor `verify` exists and both
gatekeepers improvised — three separate handoffs say so. Add one with `ready: ["bun x tsc --noEmit"]`
and an `e2e` block whose `ready` builds a throwaway repo and walks `mission new --stub` -> `open
--dry-run` -> `step` -> `close`; that single recipe would have caught F1 (`mission close` blocked by
`.factory/claim`) on the first run instead of the last. Pair it with a `--yes` flag on `install` and
`uninstall`, which W1, W3 and the Orchestrator all asked for — without it every check of the two
most important commands in the product needs a hand-timed pty and arrow-key choreography.

## r1 Validator

Driver: the CLI itself, no UI. `.factory/factory.yaml` now exists but carries `verify` and `e2e`, no
`e2e.ready` — the app is a CLI, so the CLI in temp repos under `/tmp` stayed the driver. Every run
used `HOME=/tmp/valhome.Mcxd`, seeded with the machine's `vars: {codegraph: codegraph}` and nothing
else; the real `~/.factory/projects` still lists only the six real projects, none of my temp repos.
`/opt/ed/ivy` was never written to (tree clean apart from the Orchestrator's untracked
`handoffs/accept-r1.md`) and `/opt/ed/igs` was read only — `agents.md`, no command run against it.

`install --yes` and `uninstall --yes` removed the pty entirely: every install and uninstall below ran
with `< /dev/null` and stdout piped, exit 0, no prompt.

`bash scripts/e2e.sh` -> **exit 0**, `e2e: ok`, single run, ~40 s.

A clone at `/tmp/ivyco2` (`git clone -b mission/refit`, `node_modules` symlinked) carried two driving
edits the real registry cannot express: a scratch `zzmcp` mcp part with `default: true`, and
`parts/codegraph/part.yaml` flipped to `default: true`. Both exist only because `--yes` cannot select
a non-default part; neither touches linker, uninstall or snippet code. Product code was not edited.

### Contract — round 1 validate-kind assertions

| id | result | evidence |
|----|--------|----------|
| A-R1-1 | pass | Round 0's F1 scenario rerun exactly at `/tmp/r1f1`: fresh repo, one `init` commit, no `.gitignore` at all, `git check-ignore .factory/claim` -> exit 1. `mission new demo --workflow chore --no-open` -> 0, printed `ignored  .factory/claim added to .gitignore and committed`; `check-ignore -v` -> `.gitignore:1:.factory/claim`. The commit `0b4ac0d 🧹 chore: ignore .factory/claim` holds **one file**: `git show --name-only --format=''` -> `.gitignore`, stat `1 file changed, 1 insertion(+)`. Walked grill/implement/merge, `mission close demo` -> **0**, four `·` lines, back on `main`, `git status --porcelain` empty. Graph: `54638cd 📦 chore: close mission demo` on `mission/demo`, then `afc2e60 🔀 merge`, then `707ca79 🏗️ chore: close mission demo` on main. Variants: existing clean `.gitignore` (`/tmp/r1giA`) -> line appended, commit again holds only `.gitignore`; already-ignored repo (`/tmp/r1giC`) -> no `ignored` line, no commit, no duplicate. `scripts/e2e.sh` asserts the same through the stub-promotion path |
| A-R1-2 | pass | `/tmp/r1snp2`, `AGENTS.md` with a hand-written `## Important Files` holding `` - Terminology: `docs/terminology.md` `` and `- House rules: docs/house.md`. `install --yes` -> 14 parts; `git diff` is `-- Terminology: \`docs/terminology.md\`` / `+- Terminology: @docs/terminology.md` **in place** (position kept, still above House rules) plus three appended lines. Manifest: `terminology.snippet = {file, section, line: "- Terminology: @docs/terminology.md", replaced: "- Terminology: \`docs/terminology.md\`"}`; the other three carry no `replaced`. `uninstall --yes` -> `cmp /tmp/r1snp2.orig AGENTS.md` silent. `/tmp/r1idem` pushed it further: a backticked terminology line **and** an `@docs/roadmap.md` line, both replaced in place (`@` -> backtick for roadmap), then `install --yes` again and `update` -> counts stay 1/1/1/1, file byte-stable; `uninstall --yes` -> `cmp` silent, both originals restored |
| A-R1-3 | pass | `/tmp/r1cg`, the F5 case reproduced with codegraph itself: install wrote `.mcp.json` with the resolved `codegraph serve --mcp`, `settings.json` with `mcp__codegraph__*`, `settings.local.json` with the gate hook, `.codegraph/codegraph.db`. `uninstall --yes` -> `removed .claude/settings.json, nothing left in it`, same for `settings.local.json` and `.mcp.json`; all three **gone**, manifest gone, `.codegraph` gone, `.claude/` an empty dir. `/tmp/r1mcp` repeated it with a scratch mcp part, same three deletions. `/tmp/r1snp2` (no mcp part at all) -> both settings files deleted, no `.mcp.json` ever created. Not one `{}` or `{"mcpServers":{}}` left anywhere |
| A-R1-4 | pass | `/tmp/r1yes`, `install $R --yes < /dev/null` piped to a file -> exit **0**, `Done. 14 parts installed.`, no clack glyph in the log. Manifest holds exactly the 14 `default: true` parts (`grep -l '^default: true' parts/*/part.yaml` -> 14; the three absent are archify, browse, codegraph). `uninstall $R --yes < /dev/null` -> exit **0**, `Done. 14 parts removed.`, `.claude/` empty. The "plus what is already installed" half at `/tmp/r1yes2`: codegraph installed from the clone, then the real registry's `install --yes` (where codegraph is `default: false`) kept it — `Done. 15 updated.`, `.mcp.json` intact |
| A-R1-9 | pass | `parts/roadmap/part.yaml` snippet line is `` - Roadmap: `docs/roadmap.md` `` — backticks, no `@`. ivy `AGENTS.md` `## Important Files` is exactly four lines: `@.claude/docs-format.md`, `@docs/terminology.md`, `` `docs/roadmap.md` ``, `@.claude/code-format.md`; `grep -c Roadmap` -> **1**. igs `agents.md` (same inode as `AGENTS.md`, tracked as `agents.md`) lines 152-157: `@docs/terminology.md`, `@.claude/docs-format.md`, `` `docs/roadmap.md` ``, then igs' own `- Finished tasks:` and `- Unit reference example:`, then `@.claude/code-format.md`; `grep -c Roadmap` -> **1**, and its position above the project's own lines shows the old `@` line was rewritten in place, not appended. Three `@` lines in each file, terminology / docs-format / code-format, in both projects |

### Findings

| id | verdict | finding | blast | effort | conf |
|----|---------|---------|-------|--------|------|
| - | R1-1 | Two missions running in parallel worktrees can never close. `/tmp/r1par`: `mission new p1 --worktree` then `mission new p2 --worktree`, both walked to `merge`. `close p1` -> exit 1, `✗ dirty outside the mission folder, commit or stash first: .factory/missions/2026-09-05-p2/state.json, .factory/missions/2026-09-05-p2/workflow.yaml`; `close p2` names p1's two files symmetrically. Deadlock: each mission's untracked folder is dirt to the other, and the mission folders share the main checkout by design. Deleting p2's folder let p1 close immediately, which pins the cause. The claim is correctly absent from both lists, so this is not F1 coming back — it is the same dirty scan needing `.factory/missions/*` treated the way `.factory/claim` now is. `--worktree` exists so two missions can run at once; today the second one costs you the first | wide | M | high |
| - | R1-2 | `mission new` appends its ignore line to a `.gitignore` the human has already modified, then cannot commit it, and `mission close` later refuses on that file. `/tmp/r1giB`: committed `.gitignore` plus an uncommitted `dist/` line, `mission new b` -> `ignored  .factory/claim added to .gitignore` (no "and committed"), file now carries the Factory's line mixed into the human's edit; after the walk, `mission close b` -> exit 1, `✗ dirty outside the mission folder, commit or stash first: .gitignore`. The refusal does not say that part of that dirt is the Factory's own line, and the human cannot commit theirs without shipping ours. The documented deviation covers the write; the close-side consequence is the sharp edge. Either leave a dirty `.gitignore` alone and print the line to add, or exclude the claim line from the dirty scan | narrow | S | high |
| - | R1-3 | After a full `uninstall --yes`, `status` reports `2 installed`. `/tmp/r1yes`: manifest deleted, `.claude/` empty, yet `roadmap docs/roadmap.md` and `terminology docs/terminology.md` show `● installed` because the scanner falls back to file detection and the two `skipIfExists` templates are project-owned and stay by design. The footer reads `2 installed, 0 modified, 15 available` on a project with nothing installed. Cosmetic, but it is the first thing a human runs after an uninstall to check the uninstall worked | narrow | S | high |
| - | R1-4 | Uninstall's new `dropEmptied()` cleans the settings and mcp files but not the agent file it created. `/tmp/r1mcp` and `/tmp/r1cg` had no `AGENTS.md` before install; install created one to hold the snippets, uninstall stripped every line and left a **0-byte `AGENTS.md`** plus an empty `.claude/` directory in a repo that had neither. Same class as F5, one `stat` short of finished | narrow | S | high |
| - | R1-5 | `mission new` on a claimed checkout without a tty prints the worktree question and exits **0** having done nothing. `/tmp/r1giA` with mission `a` open, `mission new a3 --workflow chore --no-open < /dev/null` -> the `◆ Work a3 in a worktree at ../r1giA-a3?` prompt, then exit 0, no branch, no folder. Every other refusal in this family exits 1 with one `✗` line; a script that reads the exit code is told the mission exists. The prompt is the documented exception, the exit code is not | narrow | S | med |

### Tooling

`scripts/e2e.sh` plus `--yes` closed the whole T1 gap: this round needed no pty, no arrow keys and no
hand-timed `script -q /dev/null`, and the one e2e run reproduced the F1 walk end to end in 40 seconds.
What is still missing is the `e2e.ready` step the Validator brief asks for first — `.factory/factory.yaml`
has `verify` and `e2e` but no `ready`, so there is no cheap "is this thing drivable" check before the
expensive one. Add `ready: ["bun x tsc --noEmit"]` alongside them. The gap this round actually paid for
is a second one: `install --yes` cannot select a non-default part, so validating codegraph or archify
still means editing a throwaway clone of the registry. A `--parts a,b` flag on `install` would have
saved that clone and would let `scripts/e2e.sh` cover the mcp and recipe paths, which is exactly where
F5 and F6 lived.

## r2 Validator

Driver: the CLI itself, no UI. `.factory/factory.yaml` still has `verify` and `e2e`, no `e2e.ready`
— reachability was taken from `bun src/cli.ts status /opt/ed/ivy` -> exit 0, `15 installed, 0
modified`. Every run below used `HOME=/tmp/valhome.IhCf` (scratch, seeded only with
`vars: {codegraph: codegraph}`); the real `~/.factory/projects` still lists the same six real
projects and **zero** `/tmp` entries. `/opt/ed/ivy` was never written to by a command (tree carries
only the Orchestrator's untracked `handoffs/accept-r2.md`). No product code was edited, nothing
committed. Nine temp repos under `/tmp`, all built by the same `git init -b main` + one `init` commit.

`bash scripts/e2e.sh` -> **exit 0**, `e2e: ok`, one run. `bun x tsc --noEmit` -> **exit 0** (run
because I am the only gatekeeper this round).

### Contract — round 2

| id | result | evidence |
|----|--------|----------|
| A-R2-1 | pass | **Order A**, `/tmp/r2par1`: `mission new p1 --workflow chore --no-open` -> 0 (main checkout on `mission/p1`), `mission new p2 ... --worktree` -> 0 (`/private/tmp/r2par1-p2`), `git worktree list` shows both. Walked grill/implement/merge in each. `mission close p1` from the main checkout -> **0**, four `·` lines (`committed … on mission/p1`, `merged mission/p1 into main`, `committed … on main`, `claim cleared`) while `.factory/missions/2026-09-06-p2/` was untracked dirt beside it. `mission close p2` from the worktree -> **0**, `committed … on main`, `worktree removed`. Main checkout on `main`, `git status --porcelain` **empty**. **Order B**, fresh `/tmp/r2par2`, worktree mission closed first: `close q2` -> **0** with `.factory/missions/2026-09-06-q1/` still untracked in the main checkout, then `close q1` -> **0**; tree empty, `git ls-files .factory/missions` holds all four files (q1 and q2 `state.json` + `workflow.yaml`). **Exact r1 R1-1 shape**, `/tmp/r2par3`, *both* `--worktree` (the pair that deadlocked in round 1): `close w1` -> **0**, `close w2` -> **0**, tree empty. **Counter-case**, `/tmp/r2dirty`: committed `file.txt` modified plus untracked `stray.txt`, mission walked to merge, `mission close d1` -> exit **1**, `✗ dirty outside the mission folder, commit or stash first: file.txt, stray.txt` — `?? .factory/` was in the same `git status` and is correctly not named |
| A-R2-2 | pass | `/tmp/r2bare`, `ls -a` before install = `.git README.md`, no `AGENTS.md`, no `.claude`. `install --yes < /dev/null` -> **0**, `Done. 14 parts installed.`, creates a 162-byte `AGENTS.md` holding only `## Important Files` + four snippet lines. `uninstall --yes < /dev/null` -> **0**, `Done. 14 parts removed.` and four `removed …, nothing left in it` lines for `settings.json`, `settings.local.json`, `AGENTS.md`, `.claude/`. `ls -aR` after: `.git README.md docs` — `AGENTS.md` GONE, `.claude` GONE. Survives the fix's own churn: `/tmp/r2upd` did install -> `update` -> `install --yes` again -> `uninstall --yes`, both still removed, so the `created` flag carries. **Counter-case**, `/tmp/r2keep`: committed hand-written `AGENTS.md` plus `.claude/mine.json`, install then `uninstall --yes` -> `AGENTS.md` PRESENT and `cmp` against the pre-install copy **silent**, `.claude/mine.json` PRESENT with its bytes, `.claude/` kept, no `removed AGENTS.md` line printed. Two more no-loss cases: a factory-created `AGENTS.md` the human appended `## My Notes` to survives with the section intact (`/tmp/r2edit`), and a factory-created `.claude/` the human dropped `foreign.json` into survives (`/tmp/r2for`) |
| A-R2-3 | **fail** | Roadmap half **passes**: `docs/roadmap.md` carries `` - [ ] `status` counts `skipIfExists` templates as installed when there is no manifest (2026-09-05-refit) `` (R1-3) and `` - [ ] `mission new` on a claimed checkout with no tty exits 0 having done nothing, it should refuse (2026-09-05-refit) `` (R1-5). README half **fails**: `awk 'length>120' README.md \| wc -l` -> **21**, lines 34-54, every one **127 chars**, the whole Commands table. The four prose lines the handoff names (5, 24, 92, 110) are indeed wrapped, so V-nit itself is fixed, but the assertion as written is not met. The handoff's reason ("a markdown table row cannot wrap") does not hold for the width: longest cell content is 38 (`` `step add <step> --after X --reason R` ``) and 72 (`Create the mission folder, workflow copy, \`state.json\`, branch and claim`), so the tightest legal row is `2+38+3+72+2` = **117 chars**. The 127 is padding, not content |

### Findings

| id | verdict | finding | blast | effort | conf |
|----|---------|---------|-------|--------|------|
| A-R2-3 | fail | README lines 34-54 are 127 chars each, over the 120 the assertion names. Not a wrapping problem: the Commands table is padded ~10 columns wider than its widest cell, and repadding it to the content (`Command` column 38, `Purpose` column 72) puts every row at 117. One reformat of one table, no wording touched | narrow | S | high |
| - | pass | R1-1 is genuinely fixed, not just for the reported shape. Both `--worktree` (the round 1 deadlock), one-in-a-worktree, and both close orders all reach exit 0 with a clean tree, and the refusal still fires on real dirt with the mission folder correctly excluded from the named list | narrow | - | high |
| - | pass | R1-4's deletes are conditional, not unconditional. `AGENTS.md` is removed only when the snippet removals left it blank *and* the install created it; a human-written file, a human-appended section, and a foreign file in a factory-created `.claude/` all survive byte-identical. The snippet round trip regression (backticked `docs/terminology.md` in a hand-written `## Important Files`) still restores the original line in place — `cmp` silent | wide | - | high |
| - | fail | `uninstall` still leaves a `docs/` it created. `/tmp/r2bare` had no `docs/`; after install/uninstall the repo carries `docs/roadmap.md` and `docs/terminology.md` and `git status` reads `?? docs/`. Same argument that just retired the 0-byte `AGENTS.md` applies to the `skipIfExists` templates: if nothing was there before and the human never touched them, an uninstall that claims `nothing left in it` should take them too, or the message should say what it is leaving. Cousin of R1-3, which is already on the roadmap | narrow | S | med |
| - | fail | `mission close` leaves the mission branch behind. `/tmp/r2par1` after both closes: `git branch --list` -> `main`, `mission/p1`, `mission/p2`. The worktree is removed and the merge lands, so the branch is pure residue; a project running one mission a day accumulates them. Cosmetic, but it makes `git branch` useless as a "what is open" answer, which is what a human reaches for before `mission list` | narrow | S | med |
