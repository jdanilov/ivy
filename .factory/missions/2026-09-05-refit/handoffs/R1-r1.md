Step: R1
Done: F1 close skips `.factory/claim` in the dirty scan; `ensureClaimIgnored` adds the ignore line on `mission new` and stub promotion and commits that one path when `.gitignore` was clean
Done: F2 `writeSnippet` replaces a section line naming the snippet's path (`@path` or backticked), original kept as `snippet.replaced`, restored by `removeSnippet`; update compares snippets by `file|section|line`
Done: F5 `dropEmptied()` deletes `.claude/settings.json`, `.claude/settings.local.json`, `.mcp.json` when `{}` or `{"mcpServers":{}}`
Done: F6 archify init adds `.claude/skills/archify/` to `.gitignore` unless ignored
Done: V1 `applyPart` in `update.ts` for both loops
Done: T1 `--yes` on install and uninstall; `.factory/factory.yaml` with `verify` and `e2e`; `scripts/e2e.sh` 51 lines walks install, stub, open, gate, steps, close, uninstall
Done: H1 `parts/code-format`, 11 lines, snippet `@.claude/code-format.md`, installed on ivy and igs
Done: item 10 roadmap snippet is backticks; old `@` line replaced in place in both projects
Done: H2/F7 roadmap lines; F4 AGENTS.md `--skip` on update only, README part table and snippets paragraph
Acceptance: A-R1-1 pass, A-R1-2 pass, A-R1-3 pass, A-R1-4 pass, A-R1-5 pass, A-R1-6 pass, A-R1-7 pass, A-R1-8 pass, A-R1-9 pass
Commands: `bash scripts/e2e.sh` -> 0; `bun x tsc --noEmit` -> 0; status ivy 15 installed 0 modified; status igs 13 installed 0 modified, known codegraph conflict row
Deviations: `ensureClaimIgnored` commits `.gitignore` (`🧹 chore: ignore .factory/claim`) because a dirty `.gitignore` blocked close; only when the file was clean before, else it writes the line and says so
Deviations: chore workflow has no gate, e2e opens one on `grill` by hand to exercise `gate open|answer`
Issues: igs left dirty: `.claude/code-format.md`, manifest, `agents.md` (Code format line added, duplicate `@` pair gone); other dirty files there are the human's
Faster: `install --yes` cannot pick a non-default part; a `--parts a,b` flag would make opt-in parts scriptable
