Step: W1
Done: `src/types.ts` gains `Snippet`, `Recipes`, `Part.snippet|recipes|vars`, `ManifestPart.snippet|uninit|initAt`.
Done: `src/core/config.ts` new: `~/.factory/config.yaml` reader, missing or malformed file is `{}`.
Done: `src/core/recipes.ts` new: `resolvePart` (config var over part default over refusal, in mcp command/args, hook commands, both recipe lists; command prefix split into command plus leading args), `runInit` (once, `initAt` gate, refuses `✗ <part> init: <line> -> <code>`), `runUninit` (`◈` and continue).
Done: `src/core/linker.ts` gains `writeSnippet` (AGENTS.md → CLAUDE.md → create AGENTS.md, section appended at EOF when missing, line appended at section end, deduped) and `removeSnippet` (recorded file, drops an emptied section plus the blank the install added); `linkPart` records `uninit`.
Done: `src/core/registry.ts` validates `snippet`, `recipes` and `vars`.
Done: install, update and uninstall resolve vars, write/remove snippets with one printed line each, and run init/uninit; a failing init writes the manifest and then refuses, leaving the part linked.
Done: `CLAUDE.md` part.yaml section documents `snippet`, `recipes`, `vars`, the substitution rule and the manifest records.
Undone: `src/commands/status.ts` untouched, the spec asks for no snippet output there.
Commands: `bun src/cli.ts update /tmp/w1d --skip ...` -> 1 with `✗ zz-scratch init: exit 3 -> 3`, symlink present, no `initAt`, rerun after the fix ran init once (A-RCP-1, A-RCP-2)
Commands: `bun src/cli.ts update /tmp/w1e --skip ...` -> 1 with `✗ part zz-scratch uses ${missing} and nothing defines it`, -> 0 once the config defined it (A-VAR-1)
Issues: `install` and `uninstall` cannot be driven by a plain pipe, clack exits without selecting. They work under a pty: `{ sleep 2; printf 'a'; sleep 1; printf '\r'; sleep 2; printf '\r'; sleep 5; } | script -q /dev/null bun src/cli.ts install <dir>`.
Issues: temp repos `/tmp/w1a`..`/tmp/w1e` and scratch home `/tmp/fh-w1` left on disk for the Validator.
Deviations: manifest also gains per-part `uninit` (resolved lines), needed for `update` to uninit a part the registry no longer ships.
Deviations: `uninstall` removes hooks, mcp and settings from the manifest entry instead of the live part.yaml, so var-resolved commands match what was written.
Deviations: `update` removes the previously recorded hook entry when the resolved command changed (A-VAR-2).
Deviations: recipe lines are echoed dim as `$ <line>` before running.
Faster: a `--yes` flag on install and uninstall would drop the pty dance from every future check.
Acceptance: A-SNP-1 pass, A-SNP-2 pass, A-SNP-3 pass, A-SNP-4 pass, A-SNP-5 pass, A-RCP-1 pass, A-RCP-2 pass, A-RCP-3 pass, A-VAR-1 pass, A-VAR-2 pass
