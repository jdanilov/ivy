Step: W2
Done: `MissionState.status` gains `stub`, `branch` becomes `string | null`, `readState` defaults branch to null for stubs
Done: `mission new <name> --stub` writes folder, workflow copy, `handoffs/`, `intent.md` skeleton and `state.json` with `status: stub`, no branch, no claim, no auto-open; refuses when the folder exists
Done: `promoteMission` in `src/core/mission.ts`: `mission open` on a stub refuses on a foreign claim, else branches from HEAD, checks out, writes the claim, sets status open, then spawns as usual
Done: `notStub` guard wired into `step start|done|skip|add|loop`, `gate open|answer`, `handoff save`, `mission adopt|resume|close`
Done: `resolveMission` falls back to a lone stub so the stub refusal surfaces without `--mission`
Done: `mission list` shows stubs with `○` and `stub` in the state column, sorted after open missions
Done: `existingProjects()` in `src/core/projects.ts`, used by `mission list` and `gate list`; `loadProjects` unpruned
Done: `mission close` commits a dirty mission folder on the mission branch as `📦 chore: close mission <name>` before leaving the branch, refuses naming paths dirty outside the folder, rerun idempotent
Done: `README.md` command table and `CLAUDE.md` command block plus three new mission invariants
Issues: `git()` in `src/core/mission.ts` returns `out.trim()`, which eats the leading status column of the first `git status --porcelain` line. Worked around with `git diff --name-only HEAD` plus `git ls-files --others --exclude-standard`.
Issues: no `.factory/factory.yaml` in this repo, so no `verify` recipe; fell back to `bun x tsc --noEmit` and `bun src/cli.ts status /opt/ed/ivy`.
Issues: W1 left no `handoffs/W1*.md` (session was not bound to the mission, fixed by adopt).
Deviations: `src/core/args.ts` adds `stub` to `BOOLEAN`; `src/ui/format.ts` adds the `stub` state column to `missionRow`.
Deviations: `resolveMission` returns a lone stub when no mission is open, needed for A-STB-2.
Faster: a `gitRaw()` sibling to `git()` that does not trim would fix the porcelain trap for every caller.
Acceptance: A-STB-1 pass, A-STB-2 pass, A-STB-3 pass, A-STB-4 pass, A-CLS-1 pass, A-CLS-2 pass
