Both steps are committed on `mission/refit`.

Key files: `/opt/ed/ivy/parts/{roadmap,codegraph,archify,retro}/`, `/opt/ed/ivy/parts/docs-format/docs-format.md`, `/opt/ed/ivy/parts/mission/agents/Worker.md`, `/opt/ed/ivy/parts/mission/skill.md`, `/opt/ed/ivy/AGENTS.md`, `/opt/ed/ivy/docs/roadmap.md`, `/opt/ed/ivy/docs/terminology.md`, `/opt/ed/ivy/README.md`.

```
Step: W3+W4
Done: parts/roadmap (fixture, default, skipIfExists, snippet) and template docs/roadmap.md
Done: terminology and docs-format parts carry @path snippets under ## Important Files
Done: docs-format.md rewritten to 23 lines, reasons then rules, glyph table kept, example gone
Done: parts/critic deleted, no reference left in parts, presets, src, README.md, AGENTS.md
Done: parts/codegraph (mcp, default false, pinned var, gate.sh with ${CODEGRAPH:-codegraph}, init/uninit recipes, mcp__codegraph__* allow)
Done: parts/archify vendored from tt-a1i/archify @ d8e4daf2610d512821365f41b139d874b29efe81, sha on line one of part.yaml, model opus, skill paths rebased on $CLAUDE_PROJECT_DIR/.claude/skills/archify/
Done: CLAUDE.md -> AGENTS.md trimmed (install narrative and part-type table dropped), CLAUDE.md is the single line @AGENTS.md, three snippet lines landed via update
Done: docs/roadmap.md in ivy with the five carried items; docs/terminology.md Gate, Question, handoffs rows depluginised, Stub, Snippet, Roadmap, Retro sweep rows added
Done: README.md part table, --stub row, snippets-and-vars paragraph
Done: Worker.md rewritten, 39 lines, goal first, exactly three guardrails
Done: canonical handoff template (Step, Done, Acceptance + optional-fields line) in Worker, mission skill.md, Investigator, Summarizer, Verifier, Validator
Done: function hooks unwired: preset.yaml plugins gone, Preset.plugins and the --plugin-dir loop gone, no agent lists mcp__factory__ask, mission skill Questions section is three lines
Done: every prompt dropped its docs-format and terminology sentences; Verifier and Validator each carry the HOME=$(mktemp -d) line with no removal
Done: parts/retro (skill, default true, opus, 54 lines) covering sweep, ordering, one item at a time, four answers, prune, delete-empty, commit, dirty-tree refusal
Done: AGENTS.md roles paragraph names /retro and the line caps
Undone: parts/permissions/part.yaml is in the W3 file list but no bullet says what to change; left unchanged
Undone: A-ARC-2 cannot pass with the two specced files
Commands: codegraph uninit --help -> 0, decided --force (there is no --yes)
Commands: node .claude/skills/archify/bin/archify.mjs doctor -> 1, "28 required files missing"
Commands: first uninstall pty attempt -> 143, timed out at 120s; W1's 'a' plus two returns sequence worked
Issues: archify's doctor requires 28 files (renderers/, schemas/, examples/, references/, recipes/, delta/, assets/, bin/*). Two files make a non-functional skill. The engine cannot express a directory vendor: files[].source is per-file and gets hashed, so the full package is ~100 entries in part.yaml (2.2MB without test/ and examples/*.html). Three options for you: (1) ship the two files as specced and drop A-ARC-2, (2) add directory-symlink support to linker.ts in a follow-up step and vendor archify/ minus test/, (3) drop the archify part and roadmap it. I took (1), nothing else was reversible.
Issues: commit 47cac59 (yours, mid-step) swept my staged `git rm parts/critic` and `git mv CLAUDE.md AGENTS.md` into "orchestrator notes for the refit retro". Net state is correct, but those two changes are not in my W3 commit.
Issues: ~/.factory/config.yaml written on this machine with vars.codegraph: codegraph, not committed. The temp check used HOME=$(mktemp -d) with the same value, so ~/.factory/projects has no temp path.
Issues: ivy had a stale dangling symlink .claude/skills/critic/scripts/fork-critic.ts from an older critic layout that update could not claim; removed by hand (.claude is gitignored)
Deviations: codegraph uninit is `--force`, not the `--yes` the spec guessed
Deviations: Summarizer, Verifier and Validator got the full handoff block instead of "the template from spec.md", so A-HND-1 is checkable in each file
Deviations: mission skill.md merged two CLI lines and gained [--stub] to land at 119 lines
Deviations: docs/terminology.md Preset row lost ", plugins", stale after W4
Deviations: archify skill.md is 138 lines, over the 80-line "others" cap in A-PRT-8; it is vendored upstream text, excluded from the cap. Every Factory-authored prompt is within its cap.
Faster: a directory `source` in files[] (symlink the folder, hash by manifest of contents) would make a full archify vendor one part.yaml line instead of a hundred
Acceptance: A-PRT-1 pass, A-PRT-2 pass, A-PRT-3 pass, A-PRT-4 pass, A-PRT-5 pass, A-PRT-6 pass, A-PRT-7 pass, A-PRT-8 pass, A-CG-1 pass, A-CG-2 pass, A-CG-3 pass, A-ARC-1 pass, A-ARC-2 fail, A-DOC-1 pass, A-DOC-2 pass, A-DOC-3 pass, A-WRK-1 pass, A-WRK-2 pass, A-HND-1 pass, A-HND-2 unchecked, A-FH-1 pass, A-FH-2 pass, A-FH-3 pass, A-RET-1 pass, A-RET-2 pass, A-RET-3 pass
```
