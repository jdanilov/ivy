Work is committed on `mission/refit` as `f010ea8`.

Key files: `/opt/ed/ivy/parts/archify/part.yaml` (now the only file in that folder), `/opt/ed/ivy/parts/codegraph/part.yaml`, `/opt/ed/ivy/src/types.ts`, `/opt/ed/ivy/README.md`.

The upstream layout differed from what the spec assumed — `tt-a1i/archify` keeps the skill in an `archify/` subdirectory, not at the repo root, so the specced one-liner cloned successfully and then failed on `node .claude/skills/archify/bin/archify.mjs doctor`:

```
Error: Cannot find module '/private/tmp/w6repo.hWSp/.claude/skills/archify/bin/archify.mjs'
```

The recipe now clones to `.claude/skills/.archify-clone` and moves that subdirectory into place.

Step: W6
Done: `parts/archify/skill.md` and `parts/archify/bin/archify.mjs` deleted; the folder is `part.yaml` alone
Done: `parts/archify/part.yaml` rewritten — type skill, default false, `files: []`, `vars.archify: https://github.com/tt-a1i/archify`, upstream sha `d8e4daf2610d512821365f41b139d874b29efe81` on line one, three init lines (guard, clone+move, doctor) and one uninit line
Done: init guard refuses with `archify: .claude/skills/archify exists and is not empty, remove it to reinstall` when the folder is there and non-empty; an empty folder is tolerated (`rmdir` before the move)
Done: uninit is `rm -rf .claude/skills/archify .claude/skills/.archify-clone`, so a half-finished clone goes too
Done: `README.md` archify row reads "cloned per project (opt-in)"; the Snippets and vars section gains an `archify: ~/src/archify` example line and three lines naming archify as the recipe-installed, `vars.archify`-repointable part. `AGENTS.md` and `docs/terminology.md` never named archify, so neither was touched
Done: `parts/codegraph/part.yaml` `mcp.config` gains `type: stdio`; `McpConfig.config` in `src/types.ts` gains `type?: string`. Resolved output is `{"type":"stdio","command":...,"args":[...]}`, key order and all, matching igs' hand wiring on a machine with `vars.codegraph: codegraph`
Done: the scanner does report a `files: []` part with recipes as `● installed` off the manifest, not `not-installed` (`src/core/scanner.ts:80`, the existing MCP-parts-with-no-files branch). No scanner change needed
Done: A-ARC-2 walked end to end in `/tmp/w6b.Be2O` with `HOME=/tmp/w6home.tiyX` — pty install with archify alone -> 0, doctor printed 15 `[ok]` lines and "Archify is ready", `initAt` recorded; `status` -> 0 `● installed`; `update` -> 0 `Up to date.` with init not rerun; pty uninstall -> `.claude/skills/archify` gone, `.claude/skills` empty, manifest deleted
Commands: pty `install` with the spec's literal init line -> 1, `Cannot find module .../bin/archify.mjs`; decided the clone must take the repo's `archify/` subdirectory, not its root
Commands: `update /tmp/w6repo.hWSp` -> 1 with `✗ archify init: [ -z "$(ls -A .claude/skills/archify ...)" ] ... -> 1`, the guard refusing over a folder left by that first failed run
Issues: a `doctor` failure leaves the clone on disk, and the guard then blocks the `update` retry the recipe contract promises. Recovery is `rm -rf .claude/skills/archify` or `uninstall` first. Refusing over an existing folder and retrying after a partial init cannot both hold with one guard; the spec chose the guard
Issues: the clone lands 5.8 MB in ~480 files at `.claude/skills/archify`. `.gitignore` is untouched per the spec, so a project that tracks `.claude/` — igs does — takes the whole skill into its index on install. Worth a `.claude/skills/archify/` ignore line in such a project before installing
Issues: temp repos `/tmp/w6b.Be2O` (uninstalled, clean) and `/tmp/w6repo.hWSp` (guard-refusal state, holds an 11 MB clone), scratch home `/tmp/w6home.tiyX` and `/tmp/w6mcp.*` left on disk for the Validator
Issues: `.factory/missions/2026-09-05-refit/handoffs/implement.md` appeared untracked mid-step and is not mine; left uncommitted
Deviations: init is three entries, not the specced two. Upstream keeps the skill in `archify/`, so line two is `rm -rf .claude/skills/.archify-clone && git clone --depth 1 --quiet "${archify}" .claude/skills/.archify-clone && { rmdir .claude/skills/archify 2>/dev/null || true; } && mv .claude/skills/.archify-clone/archify .claude/skills/archify && rm -rf .claude/skills/.archify-clone .claude/skills/archify/test`. No explicit `rm -rf .git` — the `.git` stays with the discarded clone. `doctor` is its own line so its exit code is not masked
Deviations: the sha comment gained `(skill lives in archify/)` in place of the old `(archify/)`, since that path is now load-bearing for the recipe
Deviations: `src/types.ts` was edited, which W6's file list does not name — one optional `type?: string` on `McpConfig.config`, so the codegraph key is typed rather than smuggled through the `as unknown as McpConfig` cast
Faster: `git clone --filter=blob:none --sparse` plus `git sparse-checkout set archify` would fetch 5.8 MB instead of 11.4 MB, at the cost of a longer line and a git 2.25+ floor. Not taken
Acceptance: A-ARC-1 pass, A-ARC-2 pass
