Step: R2
Done: R1-1 `/opt/ed/ivy/src/core/mission.ts` close — the dirty scan refuses only on paths outside `.factory/`; the commit of the own folder now keys off dirt inside `rel`, not off `dirty.length`, so a close with only sibling dirt no longer runs an empty `git commit -- rel`
Done: R1-4 `writeSnippet` records `created: true` on the record whose install brought the agent file into existence (carried across update and reinstall through `prev`); new `dropCreated()` in `/opt/ed/ivy/src/core/linker.ts` deletes such a file once the snippet removals left it blank and `rmdir`s `.claude/` when it is empty; uninstall calls it after the manifest is gone
Done: V-nit `/opt/ed/ivy/README.md` rewrapped, wording untouched
Done: Roadmap `/opt/ed/ivy/docs/roadmap.md` carries the `status` skipIfExists miscount and the tty-less `mission new` exit 0
Acceptance: A-R2-1 pass, A-R2-2 pass, A-R2-3 pass
Commands: `bash scripts/e2e.sh` -> 0 `e2e: ok`; `bun x tsc --noEmit` -> 0; `bun src/cli.ts status /opt/ed/ivy` -> 0, 15 installed 0 modified
Deviations: item 3 said one README line, `A-R2-3` says no line over 120 — four prose lines were over (5, 24, 92, 110), all four rewrapped since leaving three known violations would fail the assertion. The 21-row Commands table (lines 34-54, 127 chars each) is left as is: a markdown table row cannot wrap
Issues: A-R2-1 evidence, temp repos under `/tmp` with `HOME=$(mktemp -d)`: `mission new p1` + `mission new p2 --worktree`, both chore missions walked to `merge`, `close p1` -> 0 then `close p2` -> 0, and the reverse order -> 0/0, working tree clean after both; a repo with `file.txt` modified and `stray.txt` untracked still refuses, exit 1, `✗ dirty outside the mission folder, commit or stash first: file.txt, stray.txt`. A-R2-2 evidence: repo with neither `AGENTS.md` nor `.claude/`, `install --yes` then `uninstall --yes` prints `removed AGENTS.md, nothing left in it` and `removed .claude/, nothing left in it`, both gone; the counter-case (project-written `AGENTS.md` plus `.claude/mine.json`) keeps both, `AGENTS.md` byte-identical to the original. `docs/` survives every uninstall — that is the skipIfExists templates, R1-3, already on the roadmap
Faster: the two message lines in `uninstall.ts` for `dropEmptied` and `dropCreated` are the same string; one loop over a merged list would drop three lines, but the two calls must straddle the manifest delete, so I left them apart
