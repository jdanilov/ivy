## r1 Verifier

### Assertions

| id | verdict | evidence |
|----|---------|----------|
| A-SRC-4 | pass | `readEvents()` filters file mtime and last-event age to 24h (`live.ts:64,69`), `owner()` matches cwd inside project via realpath (`live.ts:303-309`), `bound` set excludes any session named by a mission's `state.session` (`live.ts:318,330`). Note: `buildSnapshot` also gates on `sessionLive` (added by W3, `live.ts:332`), a fourth condition the assertion's wording does not mention — see finding below. |
| A-SRC-5 | pass | `caffeinated()` reads `<session>.pid`, returns true only if `process.kill(pid, 0)` succeeds (`live.ts:203-211`); a dead pid throws and returns false. |
| A-SRC-6 | pass | `parts()` calls `scanProject(dir)` directly (`live.ts:280-285`, no second scan). Ran `scanProject('/opt/ed/ivy')` standalone and diffed against `bun src/cli.ts status /opt/ed/ivy`: same 17 parts, same statuses. |
| A-TRX-1 | pass | `tails` map keyed by file path holds `offset`; every read starts at `tail.offset` (`transcript.ts:83-98`), first read starts at `max(0, size - FIRST_READ)` with `FIRST_READ = 2*1024*1024` (`transcript.ts:30,93`). |
| A-TRX-3 | pass | Mission tokens: `sumUsage(tail.usage)` over the whole array (`live.ts:254`); usage is pushed before the `isSidechain` check so sidechain lines are included (`transcript.ts:108-119`); step tokens: `sumUsage(tail.usage, from, to)` bounded by `startedAt`/`endedAt` (`live.ts:116`, `transcript.ts:138-147`). |
| A-RFR-1 | pass | `watched()` lists events dir, caffeinate dir, every project's `.factory/missions` (recursive) and every open transcript (`watch.ts:18-25`); `DEBOUNCE = 200`, `POLL = 2000` (`watch.ts:14-15`), poll runs via `setInterval` regardless of watcher state (`watch.ts:78`). |
| A-RFR-2 | pass, caveat | `render()` calls `child.destroyRecursively()` on every old child, never `.remove()` (`screen.ts:484-488`). Ran a 1500-iteration headless render loop (`createTestRenderer` + `render()` + `renderOnce()`) with `Bun.gc(true)` sampling: RSS rose 110→232MB over the first ~200 renders (allocator/buffer-pool warmup, consistent with an opentui-native cost, not a growing leak) then stayed flat, 232→235.6MB (+1.5%) over the next 1300. No test in `scripts/test.ts` exercises this; the literal "flat within 20% over the first 5 minutes" reading is not met by the warmup phase, though the destroy pattern that the assertion is really about is correct and growth does not continue. See finding below. |
| A-ACT-3 | pass | `openTab()`: `sessionLive(mission.state.session)` true → returns a toast string only, never calls `openSession` (`actions.ts:47-53`); false → calls `openSession(project, mission, await loadPreset('orchestrator'), false)`. A mission's own session is always the Orchestrator's per the existing convention, so "the mission's preset" is `orchestrator` here — matches `screen.ts:707-710` wiring the `o` key to it. |
| A-ACT-4 | pass | `killSession()` runs `pgrep -f -- "--session-id ${session}"` (`actions.ts:64`, matches `spawn.ts:50`'s own flag), no match returns `no process for …` and kills nothing (`actions.ts:69`); a repeat within `ESCALATE = 5000`ms sends `SIGKILL` instead of `SIGTERM` (`actions.ts:56,71-72`). |
| A-ACT-6 | pass | `writeCaffeinate()` rewrites the `caffeinate:` line in place, leaving the rest of the file (`config.ts:45-51`); exercised end to end by `scripts/test.ts` case "c writes the mode, keeps vars and holds only its own pid" (ran, passes): `on` leaves a live pid at `control.pid`, `auto` kills only that pid and leaves another session's pid alive, `off` kills every pid in the folder. |
| A-ACT-7 | pass | `scripts/test.ts` case "the hook holds the machine awake only as the caffeinate mode says" (ran, passes): `off` starts nothing on `SessionStart`/`UserPromptSubmit`; `on` starts on `SessionStart` and the pid survives `Stop`; `auto` starts on `UserPromptSubmit`, not on `SessionStart`, and dies on `Stop`. |
| A-ACT-8 | pass | `setAttention()` reads the mission via `resolveMission`, sets `state.attention`, calls `deviate(state, …, 'set from Mission Control')`, then `writeState` (`actions.ts:141-147`) — reason string names Mission Control as required. No dedicated test, verified by reading. |
| A-NTF-1 | pass | `arrivals(prev, next)` returns `[]` when `prev === null` (`notify.ts:7-8`); otherwise filters `next` by a `Set` of `prev`'s `inboxKey`s, which excludes `at` — an age-only change keeps the same key and is filtered out. `control.ts:33-37` seeds `seen = null` and calls `announce(snapshot)` once before `startLive`, so the first snapshot never reaches `notify`. Exercised by the `scripts/test.ts` case "a new inbox key is news, the first snapshot and an ageing item are not" (ran, passes). |
| A-NTF-2 | pass | `notify()` writes `\x07` plus the Warp `\x1b]777;notify;…\x07` sequence to `process.stdout` unconditionally, then runs `osascript` only `if (process.platform !== 'darwin') return;`, via `Bun.spawn(...).unref()` with no `await` (`notify.ts:15-27`). |
| A-DOC-1 | pass | `docs/design.md` carries the pane grid (lines 59-72), the `#3a3a3a` rule (line 77), the key bar (line 71, 101-118) and a step-colour table (lines 91-96). The table documents four kinds (human/gatekeeper/agent/technical, W3's later revision), not the "three" the assertion's prose names — the assertion is stale against the spec's own "Added after the prototype closed" section, but design.md correctly matches what the code implements (`theme.ts`'s `KIND` record), so the substance of the assertion holds. `docs/terminology.md` has all four named rows (Mission Control, Inbox, Activity, Caffeinate), matching the screen. |
| A-DOC-2 | pass | `README.md:36-37` lists `factory` and `factory menu`; `AGENTS.md:23-31` architecture block lists `src/tui/` with its files; `AGENTS.md:41` config.yaml sentence names `caffeinate`. `docs/roadmap.md` has no Mission Control or tokens line (grepped, zero matches). |
| A-DOC-3 | pass | `wc -l` on `src/tui/*.ts` + `src/commands/control.ts` = 2126, under 2200. Ran `bun x tsc --noEmit` (exit 0), `bun scripts/test.ts` (exit 0, 17 cases — one fewer than the "18 cases" the handoff claims, all pass), `bash scripts/e2e.sh` (exit 0, "e2e: ok"). |
| A-UI-4 | pass | `diffCount()` is synchronous, returns the cached `Diff` immediately and starts the `git diff --shortstat` run in a fire-and-forget promise stored in `counting` (`live.ts:175-198`); `missionRow` calls it without `await` (`live.ts:239`) and only for `status === 'open' && branch`. Confirmed live: `mission new smoke` in a scratch repo then `factory control --frames` rendered the mission row with `+1 −0` from the real `git diff --shortstat`. |
| A-VAL-1 | pass | `parts/validate/agents/Validator.md` is 76 lines, §3 "Keep the scripts, do not re-drive by hand" names writing under `.factory/validator/`, one-line-per-script index in `scripts.md`, reuse and pruning (lines 32-41). `color:` rows: Worker `blue`, Investigator `cyan`, Verifier `yellow`, Validator `yellow`, Summarizer `magenta` across `.claude/agents/*.md` and `parts/*/agents/*.md` — all match. |

### Verify recipe

`bun x tsc --noEmit` exit 0 · `bun scripts/test.ts` exit 0, 17/17 · `bun src/cli.ts status /opt/ed/ivy` exit 0, 17 installed, 0 modified.

### Findings

| id | verdict | finding | blast | effort | conf |
|----|---------|---------|-------|--------|------|
| - | slop | `handoffs/implement-Worker.md` is named identically for all three Workers (W1, W2, W3) on the `implement` step, and each one fully rewrote the file rather than appended. Git history (`63b0e89`, `5afbae4`, `609180e`) shows W1's and W2's handoff content — everything about A-SRC/A-TRX/A-RFR and A-ACT/A-NTF/A-DOC evidence — is gone from the working tree, only reconstructible from `git show <commit> -- handoffs/implement-Worker.md`. The naming rule `handoffs/<step>[-<agent>][-r<round>].md` doesn't distinguish sequential Workers on one step; the round or a sequence number should. This mission's own convention (AGENTS.md's "Mission invariants" on handoffs) ate its own evidence. | wide | S | high |
| - | slop | `implement-Worker.md`'s Issues section claims `docs/design.md` "drifts in two places I am not allowed to touch" (the help panel's `#0a0a0a` fill, no Activity-scroll key row) — but the very same commit (`609180e`) that wrote this handoff also fixed both lines in `docs/design.md`. The claim is stale/incorrect at the moment it was written; a reader trusting the handoff would think docs still need a fix they don't. | narrow | S | high |
| - | slop | `EventRow.mark` (`src/tui/model.ts:45`) is set once in `fixture.ts:157` and never read by `live.ts` (which builds `Session.last` without it) or by `screen.ts` (no `.mark` reference anywhere in rendering). Dead field invented for the fixture only. | narrow | S | high |
| - | slop | The handoff's "Commands" line claims `bun scripts/test.ts` ran "18 cases, 0 failures"; the file has 17 `await check(...)` calls and the actual run prints 17 lines. Trivial but another small inaccuracy in the Workers' own evidence. | narrow | S | high |
| - | narrow | A-RFR-2's "flat within 20%" is a real number nobody tested: no case in `scripts/test.ts` exercises it, and W1's handoff (the owner) is one of the two lost to the naming collision above. My own 1500-render headless loop shows the destroy-vs-remove pattern is correct and growth stops after warmup, but the warmup itself (110→232MB, roughly the first 150-200 renders, i.e. about the same span as "5 minutes" at a 2s poll) exceeds 20%. Worth a real 5-minute wall-clock run before trusting the number, though the underlying code is right. | wide | M | med |

