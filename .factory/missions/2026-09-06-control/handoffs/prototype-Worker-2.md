Step: prototype
Done: no painted background — `backgroundColor` gone from `createCliRenderer` and the root box, `C.bg` deleted from the palette (OpenTUI defaults both to transparent, `renderer.ts:217`, `Box.ts:63`), and the root box is `terminalHeight - 1` tall so the last terminal row is never drawn rather than drawn blank; key bar stays on `height - 2`, blank row above the header stays
Done: header reads `⌬ FACTORY` in accent plus the dim path, and `caffeinate AUTO [ON]` — mode from the setting, bracket from `awake(snap)`: ON while any mission runs under AUTO, the mode itself under ON/OFF; `c` cycles and the toast carries both
Done: round shows `↻2` in the left row and only above round 0; the MISSION detail still spells `round 2`
Done: `r` clears every pending part toggle, the `±` marks and the heading count with it; the key bar shows `r Reset` next to `↵ Apply` only while something is pending
Done: `↵` is the only way into the right pane, `→` no longer focuses; `esc` leaves from anywhere on the right, `←` leaves too except in MESSAGES where `←→` pick the answer; left bar is `↑↓ Select  ↵ Open  o Tab  x Kill  c Caffeinate  q Quit`, `a adopt` gone from both the bar and the handler, `o` toasts the tab open
Done: `marker()` is a fixed three cells (`›  ` or three spaces), so a row starts in the same column with or without the chevron; mission and session rows keep one further space, which preserves their indent under the project heading
Done: left pane draws at `leftW - 2` inside a `leftW` box with `paddingRight: 2`, so the token column stops two cells short of the divider
Done: split is 2:3 — `Math.floor(w * 0.4)` left, the rest right
Done: frames regenerated at 140x42 in /opt/ed/ivy/.factory/missions/2026-09-06-control/prototype/ — `messages.txt`, `parts.txt`, `parts-confirm.txt`, `mission.txt`, `session.txt`, plus a new `parts-pending.txt` that is the only frame where `r Reset` and the `±` marks are visible
Acceptance: `bun x tsc --noEmit` clean; `bun scripts/test.ts` exit 0, 0 failures; the full `verify` recipe (tsc, tests, `status /opt/ed/ivy`) exit 0
Acceptance: background checked twice. Static: `scripts/tui-snapshot.ts` now runs `captureSpans()` over every frame and prints each distinct non-transparent bg plus whether the last row was drawn — all six frames report `bg #b8b8b8 · last row empty`, `#b8b8b8` being `C.selBg` on the inverted row, and `session.txt` reports `bg none` since nothing is selected there. Live: a `pty.fork` run of the real `bun src/cli.ts control --fixture` at 140x42 scanned every byte it wrote — the only background SGR in the stream is `48;2;184;184;184`, no `48;2;10;10;10` and no `48;5;*` at all
Acceptance: chrome held at 100x24, 200x60 and 140x42 — key bar on `height - 2`, row `height - 1` carries no spans, only `#b8b8b8` painted (throwaway script, deleted after the run)
Acceptance: pty smoke run `python3 /tmp/burst3.py` — 403 keys (four 50-key bursts at 10 ms and 0 ms gaps, then a nine-row sweep), `r`/`o`/`x` added to the key mix, alive and drawing after every burst, the `c` toast landed in all four, `q` reaped with exit 0
Acceptance: 864 lines across `src/tui/` and `src/commands/control.ts`, under the 900 budget; one commit `55553db` on `mission/control` via `git commit -- <paths>`, only the allowed paths in it
Issues: the left pane's rule under `PROJECTS` now stops two cells short of the divider, a consequence of the padding in note 7. The right pane's rule already started one cell after it, so the two rules were never continuous — say the word if you would rather the rules ran edge to edge and only the rows were padded
Deviations: added a sixth frame, `parts-pending.txt`. The five named frames cannot show note 4 — `parts.txt` has nothing pending and `parts-confirm.txt` shows the y/n bar — so the reset key and the `±` marks had no frame to be reviewed in
Faster: `captureSpans()` from `@opentui/core/testing` gives per-span `fg`/`bg` as RGBA, which is the cheap way to assert on colour that `captureCharFrame` cannot show; scanning the raw pty byte stream for `48;2;` sequences is the matching check against the real renderer
Faster: a scratch script that imports `src/tui/*` must live inside the repo. Run it from `/tmp` and Bun resolves a second copy of `@opentui/core` from its global cache, the two module instances do not recognise each other's renderables, and every frame comes back silently empty instead of failing
