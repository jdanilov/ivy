Working tree clean, one commit on `mission/control`.

Step: prototype
Done: left pane is `PROJECTS` — `Inbox (n)`, blank, then each project with its missions and unbound sessions indented under it, blank between projects; blanks skipped by ↑↓ because selection walks a `LeftItem[]` that has no blanks in it
Done: right pane follows the selection with no `tab` key — `MESSAGES (3)`, `PARTS  ivy  15/17  ±2`, `MISSION  refit`, `SESSION  quick`; `↵`/`→` focuses it, `esc` leaves, focused selection inverts, unfocused shows the dim `›`
Done: chrome — blank row above the header, key bar on `height - 2`, blank last row, all three held at 100x24, 200x60 and 140x42; brand glyph `⌬` (width 1, header right edge lines up with the rules in the frames); `caffeinate AUTO` right-aligned, `c` cycles AUTO→ON→OFF with a toast, meaning carried as a comment on `Caffeinate` in model.ts
Done: status bar carries `TIME 14m · In 310.2K · Cached 4.1M · Out 48.0K` right-aligned with the bar shrunk to what is left (capped at a third); Inbox or a project selected shows `1 running · 1 waiting · 1 stub · 1 closed` with no bar
Done: MESSAGES detail wraps to the pane width and never `…` — gate shows `retro.md (41 lines)` then the file, question shows the full text wrapped and `tab factory-igs-quick` dim, triage shows all plan lines; the answer row sits at the foot of the pane, `←→` move, `↵` turns the item `✓ answered` and drops the count
Done: PARTS — `space` marks the row with an accent `±` right-aligned, heading counts pending; below the rule the selected part's files one per line dim, the old `part · type · pending` line gone; `↵` with pending shows `apply: uninstall browse, install codegraph   y confirm  n cancel`, `y` toasts `applied (prototype)` and clears, `n` clears the line
Done: MISSION events dropped the session id column, relative times right-aligned on their own width, verb column unchanged; detail lines kept
Done: crash root cause found and fixed — `render()` detached the old row tree with `Renderable.remove()`, which detaches without freeing the child's native `TextBuffer` or its yoga node (only `destroy()` does, `Renderable.ts:1328` vs `:1573`); ~90 rows leak per frame, and the allocator fails mid-frame with `Failed to create optimized buffer: 140x12`. `render()` now calls `destroyRecursively()` on each old child
Done: guard as well as fix — `onKey()` wraps the handler, an exception becomes a toast `error: <message>` and the stack lands in `~/.factory/control.log`; the renderer survives
Done: `scripts/tui-snapshot.ts` writes `messages.txt`, `parts.txt`, `mission.txt`, `session.txt`, `parts-confirm.txt` at 140x42 into the mission's `prototype/`; `inbox.txt` deleted
Acceptance: no `acceptance.md` exists yet — this step runs before spec, so it owns no assertion ids. Verified instead:
Acceptance: `bun x tsc --noEmit` clean; `bun scripts/test.ts` exit 0, 12 cases, 0 failures; `bun src/cli.ts status /opt/ed/ivy` prints 17 installed and the Missions block
Acceptance: crash reproduced before it was fixed — pty driver (python `pty.fork`, 140x42, real `bun src/cli.ts control --fixture`) with the old `remove()` line restored died at burst 6, key 306, with `Failed to create optimized buffer: 140x12` from `zig.ts:4331`; same driver on the fixed build: 8 bursts of 50 keys (10 ms and 0 ms gaps alternating) plus a 9-row sweep of 20 keys each, 607 keys total, alive and drawing after every burst (a `c` toast probe after each burst appeared in 6 of 8), exits 0 on `q`. Headless corroboration: 2000 renders plateau at 213 MB with destroy, grow past 425 MB and crash at ~250 with remove
Acceptance: guard proven headlessly — a snapshot proxy that throws inside `handleKey` produced `error: boom from a key` on the captured frame, the frame still contained `Mission Control`, and the stack landed in `~/.factory/control.log`
Acceptance: 846 lines across `src/tui/` and `src/commands/control.ts`, fixture 155 — both under budget; only the allowed paths touched, committed with `git commit -- <paths>`
Commands: `bun /tmp/burst.ts` exit 1 — `script: tcgetattr/ioctl: Operation not supported on socket`. macOS `script` needs its own stdin to be a tty, so it cannot be driven through a pipe; the driver was rewritten on python's `pty.fork` (`/tmp/burst.py`), which works
Issues: the spec's table does not say what the status bar shows for an unbound session. It shows the owning project's summary, on the reading that a session has no mission metrics of its own
Issues: two of the eight 0 ms bursts did not show the `c` toast in the captured tail within the 1 s window. The process was alive and drawing in both, and the toast appeared on the next probe, so this reads as the 2 s toast timer redrawing over it rather than a dropped key
Deviations: the mission row's second line (`wt … · caffeinate on`) is gone from the left pane. It was the noisiest thing there and the MISSION detail block already carries branch, worktree, session and caffeinate
Deviations: triage plan lines are one row each, not wrapped, unlike the gate body and the question. Wrapping them cost the lines the 850-line budget needed and the fixture's longest plan line is 54 characters
Faster: `python3 -c` with `pty.fork` plus `TIOCSWINSZ` is the portable way to drive the real TUI on macOS — `script -q /dev/null` refuses a piped stdin, and Bun has no pty. `/tmp/burst.py` takes about 90 seconds for 600 keys and would catch any future native-resource leak the same way
Faster: comparing RSS over 400 renders in `@opentui/core/testing` found the leak in two minutes, before any pty work. Any renderable created per frame must be destroyed, not removed — worth remembering for the wiring step, which will render on a 2 s poll
