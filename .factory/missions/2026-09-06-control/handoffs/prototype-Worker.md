Prototype is committed and runnable. Files: `/opt/ed/ivy/src/tui/{model,theme,format,fixture,screen}.ts`, `/opt/ed/ivy/src/commands/control.ts`, `/opt/ed/ivy/scripts/tui-snapshot.ts`, frames in `/opt/ed/ivy/.factory/missions/2026-09-06-control/prototype/`.

Step: prototype
Done: `@opentui/core@0.5.10` added; `src/tui/model.ts` types the snapshot with `state.json` field names (status, step, round, session, branch, worktree, gates via `gateOpen`, steps, deviations)
Done: `src/tui/theme.ts` carries the design.md palette plus two new semantic colours; `src/tui/format.ts` holds the relative-time, duration, token, id formats and the Cell/row vocabulary (truncate with `…`, never wrap, full-width invert)
Done: `src/tui/fixture.ts` exports one `Snapshot`: ivy (refit running + memory stub, 17 real part names, archify and codegraph available), igs (auth blocked on the merge gate, unbound quick session, 3-item inbox with gate body, question, 3 fix / 2 skip triage), cut (intro closed 2d ago), 10 refit events, all ages relative to `Date.now()`
Done: `src/tui/screen.ts` rebuilds the tree from state on every change: header, status bar with the two-tone bar, 45% left mission column, `│` divider, three-mode right column (Inbox, Mission, Parts), key bar per focused pane, toast for every no-op action, relayout on resize
Done: `factory control --fixture` wired through `src/cli.ts` before the banner and the project picker; bare `factory control` refuses with the given message; `--fixture` declared in `BOOLEAN` in `src/core/args.ts`
Done: `scripts/tui-snapshot.ts` writes 140x42 plain-text frames of the three modes to the mission's `prototype/` folder
Acceptance: no `acceptance.md` exists yet — this step runs before spec, so it owns no assertion ids. Verified instead: `bun x tsc --noEmit` clean; `bun scripts/test.ts` exit 0, no failing case; `bun src/cli.ts status /opt/ed/ivy` prints 17 installed and the Missions block; the three text frames render and were regenerated after the last code change; the real renderer was driven under a pty — it drew 10.7 KB, contains `Mission Control` and `RUNNING`, took `tab`, and exited 0 on `q`; key handling exercised headlessly (down×2 crosses ivy→igs and the header follows the project, `a` toasts `adopt auth`, parts `space`×2 then `↵` toasts `apply: uninstall commit, uninstall verify`, inbox `↵` marks `✓ answered` and drops the count to `Inbox (2)`).
Issues: blue is `#6b8fd9`, cyan is `#5fb0b0` — both desaturated to sit beside the olive and the amber rather than glow next to them.
Issues: the step-colour rule in the brief collides with itself (gate amber / implement blue / gatekeeper cyan, but also pending and skipped dim, which covers every fixture step). Resolved by splitting the two readings: the glyph carries the life (`✓` olive, `●` orange, `○`/`·` dim, `⊘` amber) and the name carries the role colour, with plain steps falling back to the life colour. The vertical list is visibly role-coded and the lifecycle is still legible. Worth a look in `prototype/mission.txt` — easy to flip to one-colour-per-row if you prefer the literal reading.
Issues: the column divider is a real `│` in `#232323`, not a bare gap. design.md says "no vertical box-drawing rule, a column gap does the separating"; the brief asked for a faint rule and the reference screenshot has one. A background-coloured 1-cell box would match the screenshot more closely but is invisible in the text frames, so the glyph won.
Issues: the progress bar is capped at two thirds of the width, matching the reference where the track stops well short of the fraction. The text frames cannot show the olive/`#404040` split, only the bar's extent.
Deviations: `src/tui/` plus `control.ts` is 778 lines against the 600 in the brief (fixture 154, screen 394, model 95, format 76, theme 48, control 11). The fixture is data that disappears when real files land, leaving ~624; intent's own 800-line budget for `src/tui/` holds. Cutting the remaining ~180 meant packing rows onto shared lines against the house one-field-per-line style, so I stopped.
Commands: `bun /tmp/keys.ts` exit 1 — `remove expects a renderable child object`. A red herring: a probe script living outside the repo resolves `@opentui/core/testing` to the global bun cache while the imported `src/tui/*` resolves it to `node_modules`, so two copies of `Renderable` load and `instanceof` fails across them. Anything importing `@opentui/core/testing` must run from inside the repo. `scripts/tui-snapshot.ts` does.
Faster: `captureCharFrame()` from `@opentui/core/testing` gives clean plain text with no ANSI, so `scripts/tui-snapshot.ts` is a cheap review loop for any later screen work — 3 frames in about two seconds, no tty.
Faster: a `pty.fork()` wrapper that reads for 4s, sends a key, then waits for exit is the safe way to smoke-test the attached TUI; it caught that `q` needed to resolve `run()` rather than call `process.exit`.
