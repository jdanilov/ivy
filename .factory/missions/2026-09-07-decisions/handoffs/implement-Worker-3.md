Work committed on `mission/decisions` as `ebfd77d` (code and tests) and `204c84b` (docs). Tree clean, mission folder untouched.

Key files: `/opt/ed/ivy/src/core/projects.ts` (`home`, `factoryHome`, `scopeOf`), `/opt/ed/ivy/src/core/registry.ts` (scope + directory-source expansion), `/opt/ed/ivy/src/core/linker.ts` (`hooksFileName`), `/opt/ed/ivy/src/core/mission.ts` (`registered`, `archiveMission`, `listArchived`), `/opt/ed/ivy/src/commands/install.ts` (`noProjectHolds`, scope refusals).

Step: implement
Done: `Part.scope` in types and registry, defaulting to project; `scope: global` on hook-safe-bash, permissions, commit, explain, research; a global part with a snippet or recipes is a registry error
Done: built-in `${root}` resolved in `resolvePart(part, targetDir)` — `$CLAUDE_PROJECT_DIR` for a project, `$HOME` for global; hook-safe-bash and hook-factory commands use it
Done: the home dir is the global target — `scopeOf(targetDir)` picks `~/.claude/settings.json` for hooks, `scanProject` filters the registry by scope, `install|uninstall|status|update --global` route through `home()` and never touch `~/.factory/projects`
Done: `install --parts commit <project>` refuses `commit is a global part — factory install --global`; `install --global` refuses `commit is installed in <project> — run factory update on each first`; `update <project>` unlinks a part whose scope moved, the same path as a retired one
Done: `mission archive|unarchive <name>` — closed only, `git mv` between `.factory/missions/` and `.factory/archive/`, committed `🗄️ chore: archive mission <name>` on a clean tree and left staged otherwise, idempotent both ways; `listArchived` beside `listMissions`; `mission list --all` prints archived rows dim after the closed ones
Done: roadmap bugs — `factoryHome()`/`home()` read `HOME` at call time and every other `homedir()` in `src/` is gone; `registered(main)` refuses mission work outside `~/.factory/projects`; `mission open --dry-run` writes nothing including a stub's promotion and `mission open` refuses a closed mission; `mission new` refuses with no tty on a claimed checkout; `status` counts a `skipIfExists` file as installed with no manifest (already true, now covered); `uninstall` rmdirs `docs/` when it is empty; a directory `source` expands at registry load with `<target>/<relative>` targets and the manifest holds the expanded list
Done: `scripts/test.ts` — a tripwire that refuses before its first write unless `factoryHome()` is under its temp dir, repos registered through `saveProject`, and cases for the sandbox refusal, dry run plus closed refusal, archive round trip, seeded template, emptied `docs/`, and the whole global path including `update --global`
Done: `scripts/e2e.sh` — global install and uninstall over a scratch HOME, the sandbox refusal, dry run, no-tty refusal, closed refusal, archive round trip with `list --all`, a copied registry with a directory source and a global part carrying a snippet
Done: docs — AGENTS.md architecture block (`~/.claude/`, the archive folder, `scopeOf` in core), the part.yaml `scope` and directory source, the `${root}` and scope paragraphs, `--global` in the two command families, four new invariants; README `--global` and `mission archive|unarchive` rows plus the five global parts; terminology `Archive` row and a `Part` row naming scope; `docs/roadmap.md` down to Memory and Codex; `docs/design.md` untouched
Acceptance: A-GLB-1 pass, A-GLB-2 pass, A-GLB-3 pass, A-GLB-4 pass, A-ARC-1 pass, A-BUG-1 pass, A-BUG-2 pass, A-BUG-3 pass, A-BUG-4 pass, A-BUG-5 pass, A-BUG-6 pass, A-BUG-7 pass, A-DOC-1 unchecked, A-DOC-2 pass
Undone: A-DOC-1 stays unchecked until W4 lands the `docs/design.md` keys and step-colour tables; the terminology and README clauses it names are all in
Commands: `bun x tsc --noEmit` 0, `bun scripts/test.ts` 0 (30 cases), `bun src/cli.ts status /opt/ed/ivy` 0, `bun src/cli.ts --frames` 0. `bash scripts/e2e.sh` not run — the Validator's
Issues: `/opt/ed/ivy/.claude` still carries the five now-global parts as project links; the migration is `factory update /opt/ed/ivy` first, then `factory install --global`, and until the update runs `install --global` refuses naming this project
Issues: `the hook rings only when the parent session waits on the human` failed once in five runs and passed the other four — the detached player gets a fixed 300 ms, so the case is timing-flaky, not wrong. W2's ground, left alone
Issues: `uninstall` never empties `docs/` with the parts we ship: `skipIfExists` templates are copies the project owns and stay. The rmdir only fires on a genuinely empty `docs/`, so the case tests `dropCreated` directly
Decisions:
- MEDIUM: reverted the earlier W3's hardcoded `/tmp` in `scripts/test.ts` and `scripts/e2e.sh` to `tmpdir()` and a plain `mktemp -d`, so `TMPDIR` steers both; a caller with a slow temp folder can point the run somewhere small, which a hardcoded path takes away
- MEDIUM: every CLI call in `e2e.sh` reads stdin from `/dev/null`; without it the no-tty refusal never fires when the script is run from a terminal, and the case silently passes on a prompt
- MEDIUM: no shipped part adopts the new directory `source`. `hook-factory`'s `sounds/` was the obvious candidate, but a stray `.DS_Store` would then be linked into every project's `.claude/`; the feature is covered by a scratch part instead
- MEDIUM: the global-install collision is checked against `existingProjects()` manifests at install time rather than recorded anywhere, so a project whose path is gone cannot block the user's own parts
- LOW: `install` reports the hook file it actually wrote (`printHookInfo(hooksFileName(dir))`) instead of the hardcoded `settings.local.json`, so a global install does not claim a file that does not exist
- LOW: README names the five global parts in a sentence under the parts table rather than gaining a Scope column, which would have re-wrapped every row for one bit of information
